# Configuración de pnpm para React Native

# Hoisting: true para compatibilidad con React Native
# (evita problemas con Metro bundler y dependencias nativas)
shamefully-hoist=true

# Peer dependencies: false para evitar errores con React Native 0.74
# (React Native tiene muchas peer dependencies que pueden causar conflictos)
strict-peer-dependencies=false

# Auto-install peers: true para instalar automáticamente peer dependencies
auto-install-peers=true

# Node linker: hoisted para React Native (recomendado para v0.74)
node-linker=hoisted
